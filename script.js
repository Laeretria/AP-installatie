document.addEventListener('keypress', function (e) {
    if (e.key === 'A') {
        console.log(e.key);
    } else if (e.key === 'B') {
        console.log(e.key);
    } else if (e.key === 'C') {
        console.log(e.key);
    } else {
        console.log('key unrecognized');
    }
});
